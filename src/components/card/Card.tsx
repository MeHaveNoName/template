import styles from './style.module.scss'
import { StyledCard } from './styles'

export const Card = (props) => {
    return (
        <StyledCard>
            <h2>hello</h2>

        </StyledCard>
    )
}